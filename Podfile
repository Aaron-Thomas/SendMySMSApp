# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SendMySMSApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 4.3'
    pod 'SwiftyJSON'

  # Pods for SendMySMSApp

  target 'SendMySMSAppTests' do
    inherit! :search_paths

    pod 'Alamofire', '~> 4.3'
    pod 'SwiftyJSON'

    # Pods for testing
  end

  target 'SendMySMSAppUITests' do
    inherit! :search_paths

    # Pods for testing
  end

end
